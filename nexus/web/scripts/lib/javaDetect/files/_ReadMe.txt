The pages in the /examples folder use the getJavaInfo.jar located in this /files folder to perform
Java detection. 

PluginDetect will use this jar only when absolutely necessary, to avoid starting up 
the Java runtime needlessly.